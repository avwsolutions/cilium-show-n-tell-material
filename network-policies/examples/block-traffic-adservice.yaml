apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "block-checkout"
spec:
  endpointSelector:
    matchLabels:
      app: checkoutservice
  egress:
    - toPorts:
      - ports:
        - port: "443"
          protocol: TCP
